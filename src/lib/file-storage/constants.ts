/** Default owner when tool calls omit user info. */
export const TEST_USER_KEY = "test";

/** Max download size when saving from a URL (50 MiB). */
export const MAX_DOWNLOAD_BYTES = 50 * 1024 * 1024;

/** Presigned GET URL lifetime. */
export const PRESIGNED_URL_EXPIRES_SECONDS = 60 * 15;
